PORT=5000
CORS_ORIGIN=http://localhost:3000

SUPABASE_URL=
SUPABASE_KEY=          # anon/public key (used as fallback)
SUPABASE_SERVICE_KEY=  # service_role secret key — required for storage writes

GROQ_API_KEY=

JWT_SECRET=
